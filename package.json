{
  "name": "lightweight-react-feature-flags",
  "version": "1.0.1",
  "description": "lightweight react feature flags is a lightweight and intuitive React library for managing feature flags. It provides a simple context and hook-based API to enable or disable features in your React application dynamically. This package helps you manage feature releases, perform A/B testing, and control feature visibility effortlessly.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "react",
    "feature-flags",
    "hooks",
    "context"
  ],
  "author": "Michael Kaner",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.2"
  },
  "devDependencies": {
    "typescript": "^4.3.5",
    "@types/react": "^17.0.19"
  }
}
